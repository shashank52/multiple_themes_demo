import 'package:flutter/material.dart';

/// The AppChips class is a stateless widget that displays a centered text describing the various types
/// of chips supported in Flutter.
class AppChips extends StatelessWidget {
  const AppChips({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Various types of chips supported in flutter'),
    );
  }
}
